export interface Note {
    id?: string;
    title: string;
    description: string;
    category: string;
    color: string;
}

export interface Category {
    id?: string;
    name: string;
    color: string;
}
